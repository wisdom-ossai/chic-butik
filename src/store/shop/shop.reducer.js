import { InitialShopState } from "./shop.state";

export const shopReducer = (state = InitialShopState, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
}